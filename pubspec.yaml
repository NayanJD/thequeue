name: thequeue
description: A job queue implementation using redis as cache database.
version: 0.0.1
author: Nayan Das <dastms@gmail.com>
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  dartis: ^0.5.0
  logging: ^0.11.4

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
